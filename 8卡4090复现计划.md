# CODERL+ 项目 8卡4090 复现计划

> 基于项目深度分析，制定适合8卡4090（24GB显存）的完整复现方案

---

## 一、硬件资源分析与配置适配

### 1.1 硬件对比

| 参数 | A100 80G | 4090 24G | 差异倍数 |
|------|----------|----------|----------|
| 显存 | 80GB | 24GB | 3.3x |
| 内存带宽 | 2TB/s | 1TB/s | 2x |
| FP16算力 | 312 TFLOPS | 165 TFLOPS | 1.9x |
| 价格 | ~$15,000 | ~$1,600 | 9.4x |

### 1.2 配置调整策略

**核心原则**：在保证训练效果的前提下，最大化利用4090的显存

**需要调整的关键参数**：

| 参数 | 原始配置(A100) | 适配配置(4090) | 调整原因 |
|------|---------------|---------------|----------|
| train_batch_size | 128 | 32 | 减少显存占用 |
| rollout.n | 8 | 4 | 减少生成数量 |
| max_response_length | 8192 | 4096 | 减少序列长度 |
| tensor_model_parallel_size | 2 | 4 | 增加模型并行度 |
| gpu_memory_utilization | 0.8 | 0.7 | 预留更多显存 |
| ppo_micro_batch_size | 64 | 8 | 减少微批大小 |
| ppo_max_token_len_per_gpu | 32768 | 8192 | 减少每GPU token数 |
| max_prompt_length | 1024 | 512 | 减少prompt长度 |
| ppo_mini_batch_size | 64 | 16 | 减少mini批大小 |

---

## 二、分阶段复现计划

### 阶段一：环境搭建与数据准备（1-2天）

#### 2.1 环境配置

```bash
# 1. 创建conda环境
conda create -n coderlplus python=3.10
conda activate coderlplus

# 2. 安装PyTorch（CUDA 12.1）
pip install torch==2.1.0 torchvision==0.16.0 torchaudio==2.1.0 --index-url https://download.pytorch.org/whl/cu121

# 3. 安装verl框架
cd /data/home/yizhou/CODERLPLUS
pip3 install --no-deps -e .

# 4. 安装依赖包
pip3 install anthropic math_verify loky
pip install -r requirements.txt

# 5. 验证安装
python -c "import verl; print(verl.__version__)"
```

#### 2.2 数据准备

```bash
# 1. 下载训练数据
huggingface-cli download xueniki/data_CodeRLPLUS --local-dir ./data --repo-type dataset

# 2. 验证数据
ls -la data/
# 应该看到：train_set.parquet, validation_set.parquet

# 3. 检查数据格式
python -c "
import pandas as pd
df = pd.read_parquet('data/train_set.parquet')
print(f'训练样本数: {len(df)}')
print(f'列名: {df.columns.tolist()}')
print(df.head())
"
```

#### 2.3 模型下载

```bash
# 1. 下载基座模型
huggingface-cli download Qwen/Qwen2.5-Coder-7B-Instruct --local-dir ./models/Qwen2.5-Coder-7B-Instruct

# 2. 验证模型
python -c "
from transformers import AutoModelForCausalLM, AutoTokenizer
model = AutoModelForCausalLM.from_pretrained('./models/Qwen2.5-Coder-7B-Instruct')
tokenizer = AutoTokenizer.from_pretrained('./models/Qwen2.5-Coder-7B-Instruct')
print(f'模型参数量: {model.num_parameters()/1e9:.2f}B')
"
```

### 阶段二：小规模验证训练（2-3天）

#### 2.4 创建4090适配配置

```bash
# 创建4090专用配置文件
cat > recipe/config/coderlplus_4090.yaml << 'EOF'
hydra:
  searchpath:
    - file://verl/trainer/config

defaults:
  - ppo_trainer
  - _self_

# Data configuration for 4090
data:
  train_files: ???
  val_files: ???
  train_batch_size: 32
  val_batch_size: 128
  max_prompt_length: 512
  max_response_length: 4096
  filter_overlong_prompts: true
  truncation: 'error'
  dataloader_num_workers: 0

# Rollout integration configuration
data.rollout_integration:
  enable: true
  ratio: 0.4
  buffer_size: 10000
  sampling_strategy: recent
  prompt_template: default

# Actor, rollout, and reference model configuration
actor_rollout_ref:
  hybrid_engine: true
  model:
    path: ???
    use_remove_padding: true
    enable_gradient_checkpointing: true
  
  actor:
    optim:
      lr: 1e-6
    ppo_mini_batch_size: 16
    ppo_micro_batch_size_per_gpu: 4
    use_dynamic_bsz: true
    ppo_max_token_len_per_gpu: 8192
    use_kl_loss: true
    kl_loss_coef: 0.001
    kl_loss_type: low_var_kl
    entropy_coeff: 0
    fsdp_config:
      param_offload: true
      optimizer_offload: true
  
  rollout:
    log_prob_micro_batch_size_per_gpu: 4
    tensor_model_parallel_size: 4
    name: vllm
    gpu_memory_utilization: 0.7
    n: 4
    temperature: 1.0
    max_model_len: 4096
    enable_chunked_prefill: true
    max_num_batched_tokens: 8192
    
    val_kwargs:
      n: 1
      temperature: 0.0
      do_sample: false
  
  ref:
    log_prob_micro_batch_size_per_gpu: 4
    fsdp_config:
      param_offload: true

# GRPO algorithm configuration
algorithm:
  adv_estimator: grpo
  use_kl_in_reward: false

# Disable model-based reward, use custom function
reward_model:
  enable: false
  launch_reward_fn_async: false

# Custom reward function
custom_reward_function:
  path: ???
  name: compute_score
  reward_kwargs: {}

# Training configuration for 4090
trainer:
  critic_warmup: 0
  logger: [console]
  project_name: CodeRL+_4090
  experiment_name: ???
  n_gpus_per_node: 8
  nnodes: 1
  save_freq: 20
  test_freq: 5
  total_epochs: 2
  val_before_train: true
  max_actor_ckpt_to_keep: 2
EOF
```

#### 2.5 创建4090训练脚本

```bash
cat > recipe/coderlplus_4090.sh << 'EOF'
#!/bin/bash

# 4090适配版本
model_path="./models/Qwen2.5-Coder-7B-Instruct"
project_name="CodeRL+_4090"
experiment_name="test_4090"

train_file=data/train_set.parquet
val_files=data/validation_set.parquet

LOG_DIR="logs/$project_name"
mkdir -p "$LOG_DIR"
LOG_PATH="$LOG_DIR/${experiment_name}.log"

custom_reward_path=recipe/reward/reward_function_coderlplus.py

# 4090优化配置
python -m recipe.main_coderlplus \
    --config-path=config \
    --config-name=coderlplus_4090 \
    data.train_files="$train_file" \
    data.val_files="$val_files" \
    actor_rollout_ref.model.path="$model_path" \
    custom_reward_function.path="$custom_reward_path" \
    trainer.project_name="$project_name" \
    trainer.experiment_name="$experiment_name" \
    2>&1 | tee "$LOG_PATH"

echo "======================================================="
echo "Training completed!"
echo "Check results in log file: $LOG_PATH"
EOF

chmod +x recipe/coderlplus_4090.sh
```

#### 2.6 小规模验证训练

```bash
# 1. 先用更小的配置验证可行性
cat > recipe/coderlplus_4090_debug.yaml << 'EOF'
# 继承4090配置，但进一步减小规模
defaults:
  - coderlplus_4090
  - _self_

data:
  train_batch_size: 16
  val_batch_size: 64
  max_response_length: 2048

actor_rollout_ref:
  actor:
    ppo_micro_batch_size_per_gpu: 2
    ppo_max_token_len_per_gpu: 4096
  
  rollout:
    n: 2
    max_model_len: 2048
    gpu_memory_utilization: 0.6

trainer:
  total_epochs: 1
  test_freq: 2
  save_freq: 10
EOF

# 2. 运行调试训练
python -m recipe.main_coderlplus \
    --config-path=config \
    --config-name=coderlplus_4090_debug \
    data.train_files="$train_file" \
    data.val_files="$val_files" \
    actor_rollout_ref.model.path="$model_path" \
    custom_reward_function.path="$custom_reward_path" \
    trainer.project_name="CodeRL+_4090_debug" \
    trainer.experiment_name="debug_run" \
    trainer.total_training_steps=10
```

### 阶段三：完整训练（5-7天）

#### 2.7 运行完整训练

```bash
# 1. 确认配置无误后，运行完整训练
bash recipe/coderlplus_4090.sh

# 2. 监控训练过程
tail -f logs/CodeRL+_4090/test_4090.log

# 3. 监控GPU使用情况
watch -n 1 nvidia-smi
```

#### 2.8 训练监控与调优

```python
# 创建监控脚本
cat > scripts/monitor_training.py << 'EOF'
import re
import matplotlib.pyplot as plt

def parse_log(log_path):
    """解析训练日志"""
    steps = []
    rewards = []
    losses = []
    
    with open(log_path, 'r') as f:
        for line in f:
            if 'training/global_step' in line:
                step = int(line.split(':')[-1].strip())
                steps.append(step)
            if 'critic/rewards/mean' in line:
                reward = float(line.split(':')[-1].strip())
                rewards.append(reward)
            if 'actor/loss' in line:
                loss = float(line.split(':')[-1].strip())
                losses.append(loss)
    
    return steps, rewards, losses

def plot_training_curves(steps, rewards, losses):
    """绘制训练曲线"""
    fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(12, 4))
    
    ax1.plot(steps, rewards)
    ax1.set_xlabel('Step')
    ax1.set_ylabel('Mean Reward')
    ax1.set_title('Training Reward')
    
    ax2.plot(steps, losses)
    ax2.set_xlabel('Step')
    ax2.set_ylabel('Loss')
    ax2.set_title('Training Loss')
    
    plt.tight_layout()
    plt.savefig('training_curves.png')
    plt.show()

if __name__ == '__main__':
    steps, rewards, losses = parse_log('logs/CodeRL+_4090/test_4090.log')
    plot_training_curves(steps, rewards, losses)
EOF
```

### 阶段四：模型评估（1天）

#### 2.9 运行评估

```bash
# 1. 合并checkpoint为HuggingFace格式
python scripts/merge_checkpoint.py \
    --checkpoint_path checkpoints/CodeRL+_4090/test_4090/global_step_XXX/actor \
    --output_path models/CodeRL+_4090_final

# 2. 运行评估脚本
cd benchmark_evaluation/eval
bash run.sh
```

#### 2.10 评估脚本适配

```bash
# 修改评估脚本以适配4090
cat > benchmark_evaluation/eval/run_4090.sh << 'EOF'
#!/bin/bash

# 评估脚本适配4090
model_path="../../models/CodeRL+_4090_final"
output_dir="eval_results_4090"

# 评估HumanEval
python evaluate.py \
    --model_path "$model_path" \
    --dataset humaneval \
    --output_dir "$output_dir" \
    --batch_size 16 \
    --max_length 4096

# 评估LeetCode
python evaluate.py \
    --model_path "$model_path" \
    --dataset leetcode \
    --output_dir "$output_dir" \
    --batch_size 8 \
    --max_length 4096

# 评估LiveCodeBench
python evaluate.py \
    --model_path "$model_path" \
    --dataset livecodebench \
    --output_dir "$output_dir" \
    --batch_size 16 \
    --max_length 4096

echo "Evaluation completed! Results saved to $output_dir"
EOF
```

---

## 三、显存优化技巧

### 3.1 模型层面优化

```python
# 1. 启用梯度检查点
actor_rollout_ref.model.enable_gradient_checkpointing: true

# 2. 启用参数卸载
actor_rollout_ref.actor.fsdp_config.param_offload: true
actor_rollout_ref.actor.fsdp_config.optimizer_offload: true

# 3. 使用混合精度
actor_rollout_ref.actor.fsdp_config.mixed_precision: bf16

# 4. 启用remove_padding
actor_rollout_ref.model.use_remove_padding: true
```

### 3.2 推理层面优化

```python
# 1. 减少vLLM显存占用
actor_rollout_ref.rollout.gpu_memory_utilization: 0.7
actor_rollout_ref.rollout.max_model_len: 4096

# 2. 启用分块预填充
actor_rollout_ref.rollout.enable_chunked_prefill: true
actor_rollout_ref.rollout.max_num_batched_tokens: 8192

# 3. 增加模型并行度
actor_rollout_ref.rollout.tensor_model_parallel_size: 4
```

### 3.3 训练层面优化

```python
# 1. 减小batch size
data.train_batch_size: 32
actor_rollout_ref.actor.ppo_micro_batch_size_per_gpu: 4

# 2. 使用动态batch
actor_rollout_ref.actor.use_dynamic_bsz: true
actor_rollout_ref.actor.ppo_max_token_len_per_gpu: 8192

# 3. 减少rollout数量
actor_rollout_ref.rollout.n: 4
```

---

## 四、预期时间与资源

### 4.1 时间预估

| 阶段 | 任务 | 预计时间 |
|------|------|----------|
| 阶段一 | 环境搭建与数据准备 | 1-2天 |
| 阶段二 | 小规模验证训练 | 2-3天 |
| 阶段三 | 完整训练 | 5-7天 |
| 阶段四 | 模型评估 | 1天 |
| **总计** | | **9-13天** |

### 4.2 资源预估

| 资源 | 配置 |
|------|------|
| GPU | 8 × 4090 24GB |
| CPU | 64核以上 |
| 内存 | 256GB以上 |
| 存储 | 1TB SSD |
| 预计电费 | ~$50（按$0.1/kWh） |

---

## 五、风险与应对

### 5.1 潜在风险

1. **显存不足**
   - 风险：batch size过大导致OOM
   - 应对：进一步减小batch size，增加梯度累积

2. **训练不稳定**
   - 风险：4090计算精度与A100不同
   - 应对：调整学习率，增加warmup

3. **性能下降**
   - 风险：配置调整导致效果下降
   - 应对：保留A100配置的对照实验

### 5.2 应急方案

```bash
# 方案1：使用更小的模型
model_path="Qwen/Qwen2.5-Coder-1.5B-Instruct"

# 方案2：使用LoRA减少参数
actor_rollout_ref.model.lora_rank: 32
actor_rollout_ref.model.lora_alpha: 16

# 方案3：减少训练数据
# 使用数据集的子集
python -c "
import pandas as pd
df = pd.read_parquet('data/train_set.parquet')
df_sample = df.sample(frac=0.5, random_state=42)
df_sample.to_parquet('data/train_set_sample.parquet')
"
```

---

## 六、验证清单

### 6.1 环境验证

- [ ] PyTorch版本正确（2.1.0+）
- [ ] CUDA版本正确（12.1+）
- [ ] verl框架安装成功
- [ ] 依赖包安装完整

### 6.2 数据验证

- [ ] 训练数据下载完成
- [ ] 验证数据下载完成
- [ ] 数据格式正确
- [ ] 数据量符合预期

### 6.3 模型验证

- [ ] 基座模型下载完成
- [ ] 模型参数量正确（7B）
- [ ] 模型可以正常加载
- [ ] 推理测试通过

### 6.4 训练验证

- [ ] 小规模训练成功
- [ ] 显存使用在预期范围内
- [ ] 训练曲线正常
- [ ] checkpoint保存正常

### 6.5 评估验证

- [ ] checkpoint合并成功
- [ ] 评估脚本运行正常
- [ ] 评估结果符合预期
- [ ] 结果与论文对齐

---

## 七、学习路径建议

### 7.1 第一周：基础理解

- 阅读论文，理解核心思想
- 搭建环境，准备数据
- 运行小规模验证

### 7.2 第二周：深入实践

- 运行完整训练
- 分析训练动态
- 调优超参数

### 7.3 第三周：总结提升

- 运行评估，对比结果
- 分析差异，总结经验
- 准备面试材料

---

## 八、参考资源

### 8.1 官方资源

- 论文：arXiv:2510.18471
- 代码：https://github.com/jiangxxxue/CODERLPLUS
- 模型：https://huggingface.co/xueniki/Qwen2.5-Coder-7B-Instruct-CodeRLPLUS

### 8.2 工具文档

- VeRL框架：https://github.com/volcengine/verl
- Ray文档：https://docs.ray.io/
- vLLM文档：https://docs.vllm.ai/

### 8.3 学习资源

- GRPO论文：DeepSeekMath
- PPO论文：Proximal Policy Optimization Algorithms
- FSDP文档：PyTorch FSDP

---

> 本文档最后更新：2026年6月27日
> 基于CODERL+项目代码库分析生成
